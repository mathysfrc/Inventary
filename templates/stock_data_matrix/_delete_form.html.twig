<form method="post" action="{{ path('app_stock_data_matrix_delete', {'id': stock_data_matrix.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock_data_matrix.id) }}">
    <button class="btn">Delete</button>
</form>
